

services:
  web:
    build: .
    container_name: devshowcase_web
    command: gunicorn devproject.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on: []